

function square(number) {
    return number * number;
}

let altSquare = (number) => {
    return number * number;
}

console.log("function square(number) {...}", square(5));
console.log("let altSquare = (number) => {...}", altSquare(5));
